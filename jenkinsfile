pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      environment {
        PASSWORD = credentials('SECRET')
      }
      steps {
        sh '''
          export PASSWORD="$PASSWORD"
          printenv
        '''
      }
    }
  }
}
